spring:
  datasource:
    url: jdbc:mysql://3.37.216.66:3306/fis_marc?useUnicode=true&characterEncoding=utf8
    username: fis_marc
    password: Fiserver1207!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
#      ddl-auto: create-drop
#      ddl-auto: create
      ddl-auto: update
    properties:
      hibernate:
        #        show-sql: true
        format_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace